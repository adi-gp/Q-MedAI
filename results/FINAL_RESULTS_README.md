# Final results artifacts

These files correspond to the FINAL EXPERIMENT executed during this Codex run. `final_results.json` is the authoritative structured result, `final_results.csv` is the table-friendly export, `final_config.json` records the exact configuration, and `plots/` contains PNG figures generated from actual completed results.

For a college or SIH review, start with [`matched_classical_quantum_judge_summary.png`](plots/matched_classical_quantum_judge_summary.png). It compares the classical baselines and Quantum Kernel using the same 150 training rows and held-out test set. It presents a competitive result without claiming quantum advantage or clinical validation.
