# Final results artifacts

These files correspond to the FINAL EXPERIMENT executed during this Codex run. `final_results.json` is the authoritative structured result, `final_results.csv` is the table-friendly export, `final_config.json` records the exact configuration, and `plots/` contains PNG figures generated from actual completed results.
